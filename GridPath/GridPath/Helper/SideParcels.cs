using GridPath.Models;

namespace GridPath.Helper
{
    public class SideParcels
    {
        List<Parcel> sideParcels = new List<Parcel>();
        /*public List<SideParcels> GetSideParcels(double coordinatesBeginX, double coordinatesBeginY, double coordinatesEndY) 
        {
            double firstMiddleCoordingateY =coordinatesBeginY+ (coordinatesEndY - coordinatesBeginY)/2;
            double secondMiddleCoordingateY = coordinatesBeginY + ((coordinatesEndY - coordinatesBeginY) / 2)*2;
            //TODo prevod dat do struktury
            for (int i = 0; i < 3; i++)
            {
                switch (i)
                {
                    case 0:
                        //TODO obdelniky od zakladniho Y do 1. stredoveho Y, X je stejně, popřipadně x+20, X-20
                        sideParcels.Add()
                            break;
                    case 1:
                        //TODO obdelniky od 1. stredoveho Y do 2. stredoveho Y, X je stejně, popřipadně x+20, X-20
                        break;
                    case 2:
                        //TODO obdelniky od 2. stredoveho Y do koncoveho Y, X je stejně, popřipadně x+20, X-20
                        break;
                    default:
                        break;
                }
               
            }

        }*/
    }
}
